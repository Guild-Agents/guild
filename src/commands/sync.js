/**
 * sync.js — Sincroniza el estado de tasks/ con GitHub Issues
 * TODO: implementar en Fase 6
 */

import chalk from 'chalk';

export async function runSync() {
  console.log(chalk.yellow('guild sync — En construcción (Fase 6)'));
  console.log(chalk.gray('Por ahora, sincroniza manualmente usando los comandos gh en cada transición de tarea.'));
  console.log(chalk.gray('Ver spec del proyecto para los comandos exactos.'));
}
